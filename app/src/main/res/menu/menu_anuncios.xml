<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto">

    <group
            android:id="@+id/group_sign_in" android:visible="false">
        <item android:title="@string/my_ads" android:id="@+id/menu_anuncios"/>
        <item android:title="@string/exit" android:id="@+id/menu_sair"/>
    </group>
    <group
            android:id="@+id/group_sign_out" android:visible="false">
        <item android:title="@string/login_or_register" android:id="@+id/menu_acesso" app:showAsAction="ifRoom"/>
    </group>
</menu>